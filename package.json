{
  "name": "simulator",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf dist/* && rm -rf cache && parcel app/views/index.pug --no-hmr",
    "build": "rm -rf docs && parcel build app/views/index.pug --no-source-maps --out-dir docs"
  },
  "keywords": [],
  "author": "proper-code",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "*",
    "pug": "*",
    "stylus": "*"
  },
  "dependencies": {
    "three": "^0.101.1",
    "three-obj-loader": "^1.1.3",
    "three-obj-loader-es6": "0.0.1"
  }
}
